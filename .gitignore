# Local art-direction reference. The vis is GLSL, not this clip.
assets/video/*.mov
assets/video/*.mp4
assets/video/*.webm

.DS_Store
**/*.af~lock~

/target
**/*.rs.bk
.idea/
.vscode/

node_modules
dist
src-tauri/gen

# Signing / secrets
.env
.env.*
.env.notarize
*.p12
*.pfx
*.pem
*.key
*.p8

build DMG archive/
*.dmg

# Agent handoff and machine notes — keep local, not the public tree
AGENTS.md
Agents.md
docs/SESSION.md

# One-off Windows livetype probe (USB harvest). App hunt lives in fonarch-core.
tools/windows-probe/
